# NUT upsd.conf
# This file controls the behavior of the NUT upsd server (the UPS network server)
# Example minimal configuration:

# Listen on all interfaces (or specify 127.0.0.1 for localhost only)
LISTEN 0.0.0.0 3493

# You can add more configuration options as needed for your setup.
